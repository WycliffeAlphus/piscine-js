function reverse(nb){
let newArray = [];
if (Array.isArray(nb)){
    for (let i=0;i<nb.length; i++){
        newArray.unshift(nb[i])
    }
}
let newArray1 = "";
if (typeof n==='string'){
    for (let i=0;i<nb.length; i++){
        newArray1 = nb[i]+newArray
    }
    return newArray1
}
}