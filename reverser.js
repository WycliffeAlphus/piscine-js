function reverse(nb){
let newArray = [];
if (Array.isArray(nb)){
    for (let i=0;i<length(nb); i++){
        newArray.unshift(nb[i])
    }
}
let newArray1 = "";
if (typeof n==='string'){
    for (let i=0;i<length(nb); i++){
        newArray1 = nb[i]+newArray
    }
    return newArray1
}
}